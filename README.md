# 液态玻璃效果 (Liquid Glass Effect)

一个使用 Vue3 + WebGL 实现的仿苹果 iOS 液态透明玻璃效果的项目。

## ✨ 特性

- 🌊 **WebGL 水珠效果** - 中间区域布满液态透明玻璃水珠，带有真实的折射和波纹效果
- 🎨 **双重实现方式**
  - **左侧**: 使用 `backdrop-filter` + SVG 实现的液态玻璃效果
  - **右侧**: 使用 WebGL Shader 实现的动态液态玻璃效果
- 💎 **可复用组件** - 液态玻璃按钮和卡片组件，边框平整，背景透明
- 📱 **响应式设计** - 适配不同屏幕尺寸
- 🚀 **高性能** - 使用 WebGL 硬件加速

## 🛠️ 技术栈

- Vue 3.4+
- TypeScript 5.3+
- Vite 5.0+
- SCSS
- WebGL

## 📦 安装

```bash
npm install
```

## 🚀 开发

```bash
npm run dev
```

项目将在 `http://localhost:9986` 启动

## 🏗️ 构建

```bash
npm run build
```

## 🌐 部署

项目配置了 GitHub Actions 自动部署到 GitHub Pages。

推送代码到 main 分支后会自动触发部署。

或者手动部署：

```bash
npm run deploy
```

## 📁 项目结构

```
liquid-glass/
├── public/
│   └── bg.jpeg              # 背景图片
├── src/
│   ├── components/
│   │   ├── DropletCanvas.vue          # WebGL 水珠画布
│   │   ├── BackdropGlassButton.vue    # Backdrop Filter 按钮
│   │   ├── BackdropGlassCard.vue      # Backdrop Filter 卡片
│   │   ├── WebGLGlassButton.vue       # WebGL 液态玻璃按钮
│   │   └── WebGLGlassCard.vue         # WebGL 液态玻璃卡片
│   ├── utils/
│   │   ├── webgl.ts          # WebGL 水珠效果工具
│   │   └── glassWebGL.ts     # WebGL 液态玻璃工具
│   ├── styles/
│   │   └── global.scss       # 全局样式
│   ├── App.vue               # 主应用组件
│   └── main.ts               # 入口文件
├── index.html
├── vite.config.ts
├── tsconfig.json
└── package.json
```

## 🎨 效果说明

### 中间水珠效果
- 使用 WebGL Shader 实现
- 动态折射背景图片
- 液态波纹动画
- 真实的光影效果
- 不同尺寸的水珠（30px-100px）

### 左侧 Backdrop Filter 效果
- 使用 CSS `backdrop-filter` 属性
- SVG 滤镜增强液态感
- 边框始终保持平整
- 透明模糊的玻璃质感

### 右侧 WebGL 效果
- 完全使用 WebGL Shader 渲染
- 动态折射和液态波纹
- 色散效果
- 实时光影计算

## 🔧 配置

### 修改端口号

在 `vite.config.ts` 中修改：

```typescript
server: {
  port: 9986  // 修改为你想要的端口
}
```

### 修改 GitHub Pages 路径

在 `vite.config.ts` 中修改：

```typescript
base: '/liquid-glass/'  // 修改为你的仓库名
```

## 📝 许可证

MIT
